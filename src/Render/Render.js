class Render {
    /** @param {iso2d} core */
    constructor(core) {
        this._core = core;
        this._canvas = core.canvas;
        this._verteces = [];
    }

    pushVerteces(verteces) {
    }
}

module.exports = Render;